[Unit]  
  
Description=mongodb  
After=network.target remote-fs.target nss-lookup.target  
  
[Service]  
Type=forking  
ExecStart={{ install_dir }}/bin/mongos -f {{ install_dir }}/conf/mongos.conf
ExecReload=/bin/kill -s HUP $MAINPID  
ExecStop={{ install_dir }}/bin/mongos --shutdown -f {{ install_dir }}/conf/mongos.conf
PrivateTmp=true  
  
[Install]  
WantedBy=multi-user.target 